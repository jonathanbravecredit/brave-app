import { CreditReportPipe } from './credit-report.pipe';

describe('CreditReportPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditReportPipe();
    expect(pipe).toBeTruthy();
  });
});
