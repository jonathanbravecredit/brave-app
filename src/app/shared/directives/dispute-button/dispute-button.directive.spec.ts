import { DisputeButtonDirective } from './dispute-button.directive';

describe('DisputeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DisputeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
