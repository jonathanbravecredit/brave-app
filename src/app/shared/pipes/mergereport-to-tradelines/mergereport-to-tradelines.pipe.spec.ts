import { MergereportToTradelinesPipe } from './mergereport-to-tradelines.pipe';

describe('MergereportToTradelinesPipe', () => {
  it('create an instance', () => {
    const pipe = new MergereportToTradelinesPipe();
    expect(pipe).toBeTruthy();
  });
});
