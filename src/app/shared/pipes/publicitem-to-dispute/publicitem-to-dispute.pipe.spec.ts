import { PublicitemToDisputePipe } from './publicitem-to-dispute.pipe';

describe('PublicitemToDisputePipe', () => {
  it('create an instance', () => {
    const pipe = new PublicitemToDisputePipe();
    expect(pipe).toBeTruthy();
  });
});
