import { DisputesToDisputePipe } from './disputes-to-dispute.pipe';

describe('DisputesToDisputePipe', () => {
  it('create an instance', () => {
    const pipe = new DisputesToDisputePipe();
    expect(pipe).toBeTruthy();
  });
});
