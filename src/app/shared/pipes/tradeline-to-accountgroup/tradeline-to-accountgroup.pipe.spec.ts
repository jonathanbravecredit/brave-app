import { TradelineToAccountgroupPipe } from './tradeline-to-accountgroup.pipe';

describe('TradelineToAccountgroupPipe', () => {
  it('create an instance', () => {
    const pipe = new TradelineToAccountgroupPipe();
    expect(pipe).toBeTruthy();
  });
});
