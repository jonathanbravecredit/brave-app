import { MergereportToPublicitemsPipe } from './mergereport-to-publicitems.pipe';

describe('MergereportToPublicitemsPipe', () => {
  it('create an instance', () => {
    const pipe = new MergereportToPublicitemsPipe();
    expect(pipe).toBeTruthy();
  });
});
