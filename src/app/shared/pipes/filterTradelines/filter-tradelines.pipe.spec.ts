import { FilterTradelinesPipe } from './filter-tradelines.pipe';

describe('FilterTradelinesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTradelinesPipe();
    expect(pipe).toBeTruthy();
  });
});
