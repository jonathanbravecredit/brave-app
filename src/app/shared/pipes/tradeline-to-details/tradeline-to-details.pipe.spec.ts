import { TradelineToDetailsPipe } from './tradeline-to-details.pipe';

describe('TradelineToDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new TradelineToDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
