import { FilterSubscribersPipe } from './filter-subscribers.pipe';

describe('FilterSubscribersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubscribersPipe();
    expect(pipe).toBeTruthy();
  });
});
