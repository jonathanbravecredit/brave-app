import { FindingsTransformerPipe } from './findings-transformer.pipe';

describe('FindingsTransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new FindingsTransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
