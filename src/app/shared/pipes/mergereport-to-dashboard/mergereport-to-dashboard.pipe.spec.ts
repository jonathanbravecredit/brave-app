import { MergereportToDashboardPipe } from './mergereport-to-dashboard.pipe';

describe('MergereportToDashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new MergereportToDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
