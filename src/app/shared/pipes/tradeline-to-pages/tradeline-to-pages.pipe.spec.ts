import { TradelineToPagesPipe } from './tradeline-to-pages.pipe';

describe('TradelineToPagesPipe', () => {
  it('create an instance', () => {
    const pipe = new TradelineToPagesPipe();
    expect(pipe).toBeTruthy();
  });
});
