import { TradelineToDisputePipe } from './tradeline-to-dispute.pipe';

describe('TradelineToDisputePipe', () => {
  it('create an instance', () => {
    const pipe = new TradelineToDisputePipe();
    expect(pipe).toBeTruthy();
  });
});
