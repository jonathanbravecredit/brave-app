import { TradelineToFindingsPipe } from './tradeline-to-findings.pipe';

describe('TradelineToFindingsPipe', () => {
  it('create an instance', () => {
    const pipe = new TradelineToFindingsPipe();
    expect(pipe).toBeTruthy();
  });
});
