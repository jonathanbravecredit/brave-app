import { ReasonsToPagesPipe } from './reasons-to-pages.pipe';

describe('ReasonsToPagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReasonsToPagesPipe();
    expect(pipe).toBeTruthy();
  });
});
