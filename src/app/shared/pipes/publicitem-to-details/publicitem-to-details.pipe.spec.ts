import { PublicitemToDetailsPipe } from './publicitem-to-details.pipe';

describe('PublicitemToDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new PublicitemToDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
