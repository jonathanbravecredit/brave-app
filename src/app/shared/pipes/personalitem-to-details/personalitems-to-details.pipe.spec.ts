import { PersonalitemsToDetailsPipe } from './personalitems-to-details.pipe';

describe('PersonalitemsToDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalitemsToDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
