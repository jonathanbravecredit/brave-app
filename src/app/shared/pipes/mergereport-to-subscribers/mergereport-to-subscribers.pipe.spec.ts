import { MergereportToSubscribersPipe } from './mergereport-to-subscribers.pipe';

describe('MergereportToSubscribersPipe', () => {
  it('create an instance', () => {
    const pipe = new MergereportToSubscribersPipe();
    expect(pipe).toBeTruthy();
  });
});
