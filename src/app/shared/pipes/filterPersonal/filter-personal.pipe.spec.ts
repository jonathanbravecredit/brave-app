import { FilterPersonalPipe } from './filter-personal.pipe';

describe('FilterPersonalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPersonalPipe();
    expect(pipe).toBeTruthy();
  });
});
