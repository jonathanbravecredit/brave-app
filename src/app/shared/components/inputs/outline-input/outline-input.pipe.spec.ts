import { OutlineInputPipe } from './outline-input.pipe';

describe('OutlineInputPipe', () => {
  it('create an instance', () => {
    const pipe = new OutlineInputPipe();
    expect(pipe).toBeTruthy();
  });
});
