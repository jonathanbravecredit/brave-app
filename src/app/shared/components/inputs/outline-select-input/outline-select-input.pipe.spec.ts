import { OutlineSelectInputPipe } from './outline-select-input.pipe';

describe('OutlineSelectInputPipe', () => {
  it('create an instance', () => {
    const pipe = new OutlineSelectInputPipe();
    expect(pipe).toBeTruthy();
  });
});
