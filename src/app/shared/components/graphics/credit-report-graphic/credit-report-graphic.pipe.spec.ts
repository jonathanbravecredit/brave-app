import { CreditReportGraphicPipe } from './credit-report-graphic.pipe';

describe('CreditReportGraphicPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditReportGraphicPipe();
    expect(pipe).toBeTruthy();
  });
});
