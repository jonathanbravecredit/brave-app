import { DisputeCardStatusPipe } from './dispute-card-status.pipe';

describe('DisputeCardStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DisputeCardStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
