import { SnapshotLabelPipe } from './snapshot-label.pipe';

describe('SnapshotLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SnapshotLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
