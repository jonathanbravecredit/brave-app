import { SnapshotStatusPipe } from './snapshot-status.pipe';

describe('SnapshotStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SnapshotStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
