import { FilledClosingAlertPipe } from './filled-closing-alert.pipe';

describe('FilledClosingAlertPipe', () => {
  it('create an instance', () => {
    const pipe = new FilledClosingAlertPipe();
    expect(pipe).toBeTruthy();
  });
});
