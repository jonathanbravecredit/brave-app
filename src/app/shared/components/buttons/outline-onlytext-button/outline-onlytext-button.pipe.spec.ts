import { OutlineOnlytextButtonPipe } from './outline-onlytext-button.pipe';

describe('OutlineOnlytextButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new OutlineOnlytextButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
